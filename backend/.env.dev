# ============================================================
# AI Ignite Note - 后端开发环境配置
# ============================================================
# 用途：本地开发模式，连接 Docker 中的数据库
# 使用方法：
#   1. 启动数据库: docker-compose -f docker-compose.dev.yml up -d
#   2. 启动后端: cd backend && cp .env.dev .env && npm run dev

# ============================================================
# Database Configuration
# ============================================================
# 连接到 Docker 中的 PostgreSQL (通过 localhost:5432)
DATABASE_URL="postgresql://aiignite:aiignite_password@localhost:5432/ai_ignite_note?schema=public"

# ============================================================
# Server Configuration
# ============================================================
NODE_ENV="development"
PORT=4000
CORS_ORIGIN="http://localhost:3000,http://localhost:3200"

# ============================================================
# JWT Authentication
# ============================================================
JWT_SECRET="dev-jwt-secret-key-change-in-production"
JWT_REFRESH_SECRET="dev-refresh-secret-key-change-in-production"
JWT_ACCESS_EXPIRY="15m"
JWT_REFRESH_EXPIRY="7d"

# ============================================================
# AI Provider Configuration
# ============================================================
# 至少配置一个 AI 提供商的 API 密钥

# Google Gemini (推荐 - 有免费额度)
# 获取地址: https://makersuite.google.com/app/apikey
GOOGLE_AI_API_KEY="your-gemini-api-key"

# Anthropic Claude
# 获取地址: https://console.anthropic.com/
ANTHROPIC_API_KEY=""

# OpenAI GPT
# 获取地址: https://platform.openai.com/api-keys
OPENAI_API_KEY=""

# 本地 AI 提供商 (可选)
OLLAMA_API_URL="http://localhost:11434"
LM_STUDIO_API_URL="http://localhost:1234"

# ============================================================
# File Upload Configuration
# ============================================================
MAX_FILE_SIZE=10485760
UPLOAD_DIR="./uploads"

# ============================================================
# Rate Limiting
# ============================================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ============================================================
# Logging
# ============================================================
LOG_LEVEL="debug"
